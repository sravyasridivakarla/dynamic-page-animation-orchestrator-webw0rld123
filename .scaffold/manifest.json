{
  "version": 1,
  "project": {
    "name": "backend",
    "scaffold_type": "backend",
    "tech_stack": "Context",
    "capability_name": "Page Config Drafter",
    "platform": "Mobile",
    "project_size": "Small"
  },
  "structure": {
    "directories": [
      "backend/",
      "backend/app/",
      "backend/app/core/",
      "backend/tests/",
      "backend/tests/unit/",
      "backend/migrations/",
      "scripts/",
      "docs/",
      "docs/agent-guides/"
    ]
  },
  "conventions": {
    "naming_style": "snake_case for all files and folders",
    "layout": "feature subdirectories under backend/app/ capability folder (backend/app/{cap}/feature_name/models.py)",
    "test_pattern": "test_*.py",
    "model_pattern": "<feature>/models.py",
    "schema_pattern": "<feature>/schema.py",
    "api_pattern": "<feature>/api.py",
    "service_pattern": "<feature>/service.py",
    "repository_pattern": "<feature>/repository.py",
    "reference_doc": "CONVENTIONS.md at project root"
  },
  "path_mappings": {
    "backend/app/main.py": "backend/app/`backend/main.py"
  },
  "rules": [
    "Do NOT reorganize the scaffolded folder structure.",
    "Place files in the directories indicated by path_mappings.",
    "Follow the naming conventions above for new files.",
    "Read CONVENTIONS.md at the project root for the 6-file module pattern and naming conventions.",
    "Each feature gets its own subdirectory under the capability folder. Files use short names (models.py, api.py) \u2014 NOT prefixed names."
  ]
}
