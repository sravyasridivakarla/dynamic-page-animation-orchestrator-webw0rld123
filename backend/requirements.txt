fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.12.0
httpx>=0.25.0
pytest>=7.4.0
structlog>=23.0.0
# DEC-SEC-9 CONFLICT: psycopg[binary,asyncio]>=3.1.0 is required for async PostgreSQL
# connectivity with SQLAlchemy 2.0 (validation checklist §Python-Specific) but is not
# on the approved dependency allowlist. Add psycopg to DEC-SEC-9 before deploying.
